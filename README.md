# acai-docs

Landing page for the Acai project — https://www.acaiproject.io/

Static site (plain HTML/CSS/JS), served via GitHub Pages. `index.html` is the
whole page; `styles.css` holds the design tokens and layout; `script.js` wires
up the trigger-tab switcher in the code section.
